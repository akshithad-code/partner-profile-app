import React from "react";
import PartnerProfile from "./components/PartnerProfile"; // adjust the path if needed
import "./App.css"; // keep if you still want global styles

function App() {
  return (
    <div className="App">
      <PartnerProfile />
    </div>
  );
}

export default App;
